id: WI-0011
store: local
project: PRJ-mykullzeroone/agentic-engineering-platform
type: spec
work_state: done
priority: high
title: Define the local work store on-disk format
description: |
  ADR-012 calls this the next deliverable and notes that until it exists, neither store is
  fully specified -- while .agentic/project.yaml has said work_store: local since ADR-012 was
  accepted, naming a format that did not exist. Blocks CLAUDE.md merge requirement #1, the
  branch naming rule, and converting IMPLEMENTATION_ROADMAP out of documentation.

  Created retroactively: the work store did not exist when this work began, so the first item
  it can hold is the item for building it.
required_gates:
- platform_config
branch: claude/docs-processes-review-4vyl8q
pull_request: 4
