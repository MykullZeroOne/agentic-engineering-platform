id: WI-0010
store: local
project: PRJ-mykullzeroone/agentic-engineering-platform
type: spec
work_state: ready
priority: low
title: Define the skill schema and the evidence package schema
description: |
  Two known gaps in SPEC-REGISTRIES. Skills are referenced as name@version throughout with no
  schema and .agentic/skills/ is empty. The evidence package has a shape -- the pull request
  template -- but no schema, so QA and review cannot consume it mechanically.
