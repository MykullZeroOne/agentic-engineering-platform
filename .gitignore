.DS_Store
__pycache__/
*.pyc

# Go build output
/devctl

# Agent and IDE scratch. CLAUDE.md forbids committing agent scratch output; these are
# the directories this repository has actually seen it land in.
.agent/
.idea/

# Run records: the durable copy lives in the PR body a run opens (ADR-017), not here.
.agentic/runs/
